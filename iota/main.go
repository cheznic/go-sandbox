package main

import (
	"fmt"
)

const (
	a = iota
	b = iota
	c = iota
	d = iota
)

const (
	e = iota
	f = iota
	g = iota
	h = iota
)

func main() {
	fmt.Println("Playing with IOTAs")
	fmt.Println(a)
	fmt.Println(b)
	fmt.Println(c)
	fmt.Println(d)
	fmt.Println(e)
	fmt.Println(f)
	fmt.Println(g)
	fmt.Println(h)
}
